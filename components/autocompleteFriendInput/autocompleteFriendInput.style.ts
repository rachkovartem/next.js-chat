import {makeStyles} from "@material-ui/core/styles";

export const autocompleteFriendInputStyle = makeStyles({
  userPaperNoCursor: {
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'center',
    marginTop: '5px',
  },
})



