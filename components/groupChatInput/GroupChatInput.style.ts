import {makeStyles} from "@material-ui/core/styles";

export const GroupChatInputStyle = makeStyles({
  groupChatPaper: {
    width: '100%',
    display: 'flex',
    flexWrap: 'wrap',
    gap: '5px',
    justifyContent: 'flex-start',
    marginBottom: '10px',
    padding: '5px'
  },
});